# 阿里云自动构建镜像。生产环境建议使用明确版本号，测试环境可使用 latest。
IMAGE_NAME=registry.cn-hangzhou.aliyuncs.com/jiangshitong/cloudmail-token-broker:latest

# CloudMail 上游请求超时时间。实例地址、管理员凭据、代理和 TLS 由管理端维护。
REQUEST_TIMEOUT_SECONDS=15
LOG_LEVEL=INFO

# Xiaoasi Mail Gateway。生产环境只使用服务器 PostgreSQL，不创建数据库容器。
GATEWAY_ENABLED=true
DATABASE_URL=postgresql://gateway_user:replace-with-database-password@host.docker.internal:5432/xiaoasi_mail?connect_timeout=10

# 必须替换为两条不同的高强度随机字符串，至少 32 字节。
DATA_ENCRYPTION_KEY=replace-with-at-least-48-byte-data-encryption-key
MAILBOX_SESSION_SECRET=replace-with-at-least-48-byte-mailbox-session-secret
MAILBOX_SESSION_TTL_SECONDS=1800

# 管理端登录。生产环境建议配置 ADMIN_PASSWORD_HASH；也可先使用明文 ADMIN_PASSWORD。
ADMIN_USERNAME=admin
ADMIN_PASSWORD=replace-with-strong-admin-password
ADMIN_PASSWORD_HASH=
ADMIN_SESSION_TTL_SECONDS=28800
ADMIN_COOKIE_SECURE=true
ADMIN_LOGIN_RATE_LIMIT_PER_MINUTE=10

# 邮箱业务接口按 X-API-Key 每分钟限流。
# 创建邮箱接口限流：同一个来源 IP 每分钟最多允许创建邮箱的请求次数。
MAILBOX_CREATE_RATE_LIMIT_PER_MINUTE=120
# 邮箱查询接口限流：同一个来源 IP 每分钟最多允许查询邮箱状态或验证码的请求次数。
MAILBOX_POLL_RATE_LIMIT_PER_MINUTE=600

# 用户自助注册。启用后必须配置下面的 SMTP；验证码默认 10 分钟有效，同一邮箱 60 秒内不能重复发送。
# 用户自助注册开关：true 表示用户可以在用户中心通过邮箱验证码注册，false 表示只能由管理员创建用户。
USER_REGISTRATION_ENABLED=false
# 注册验证码有效期：单位为秒，600 表示验证码 10 分钟内有效。
USER_REGISTRATION_CODE_TTL_SECONDS=600
# 注册验证码发送冷却时间：单位为秒，同一邮箱 60 秒内不能重复发送。
USER_REGISTRATION_CODE_COOLDOWN_SECONDS=60
# 注册接口来源限流：同一个来源 IP 每分钟最多允许发送验证码和提交注册的总请求次数。
USER_REGISTRATION_RATE_LIMIT_PER_MINUTE=10

# 注册验证码发信服务器地址：这里使用网易 163 邮箱的 SMTP 服务。
SMTP_HOST=smtp.163.com
# SMTP 服务器端口：465 表示使用 SSL 加密连接。
SMTP_PORT=465
# SMTP 登录账号：用于登录发信服务器的完整邮箱地址。
SMTP_USERNAME=notice@example.com
# SMTP 登录授权码：填写邮箱服务商生成的 SMTP 授权码，不是邮箱网页登录密码。
SMTP_PASSWORD=your-smtp-authorization-code
# 注册验证码发件人：用户收到邮件时显示的发件邮箱地址。
SMTP_FROM=notice@example.com
# SMTP 加密开关：true 配合 465 端口使用 SSL 加密连接。
SMTP_TLS=true

# POP3 uses the fixed public port 110; the container listens on 8110.
# POP3 服务开关：true 表示启动 POP3 取信服务，false 表示只启动 HTTP 管理端和业务接口。
POP3_ENABLED=true
# POP3 容器监听地址：0.0.0.0 表示监听容器内所有网络接口。
POP3_BIND_HOST=0.0.0.0
# POP3 容器内部监听端口：当前为 8110，由 Docker 映射为宿主机公开的 110 端口。
POP3_PORT=8110
